# Faux 'mailpw.conf'

# The Foo Files
# USER is here
foo space faux/foo/opensmtpd-users faux/foo/reload # (uses tabs)
foo passwd faux/foo/dovecot-passwd

# bar files contain USER
bar space faux/bar/opensmtpd-users faux/bar/reload
bar passwd faux/bar/dovecot-passwd

# baz files do not contain USER
baz space faux/baz/opensmtpd-users faux/baz/reload
baz passwd faux/baz/dovecot-passwd
